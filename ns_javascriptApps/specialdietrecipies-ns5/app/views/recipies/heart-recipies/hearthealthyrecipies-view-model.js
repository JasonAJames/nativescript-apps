var observableModule = require("data/observable");

function HearthealthyrecipiesViewModel() {
	var viewModel = observableModule.fromObject({
		
	});

	return viewModel;
}

module.exports = HearthealthyrecipiesViewModel;
